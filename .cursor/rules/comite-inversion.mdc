---
description: Memoria obligatoria — Monarch Capital / Terminal CEO / Comité de Inversión
alwaysApply: true
---

# Monarch Capital — memoria del agente

Antes de cambiar estrategia o código de trading, lee en este orden:
1. Este archivo
2. `PROJECT_MEMORY.json` (**memoria completa** — arquitectura, comités, pipeline, PRs, brand, pitch, live)
3. `HANDOFF.json` (snapshot ops corto)
4. `README.md` (ops + deploy)
5. `dashboard/pitch/MONARCH_CAPITAL_SHARK_TANK.md` si el trabajo es pitch/investor
6. `CEO_TERMINAL_NOW.md` si existe (estado de sesión)

## Identidad
- Producto: **Monarch Capital** (ex NexBuy)
- Web: solo terminal (`/` → `/dashboard`). Pitch en Canva/MD, no en la home.
- Brand: `#012622` / `#C5A572` · Cinzel + Outfit · corona

## Agente origen
- Nombre: **Comité de inversión IA**
- bcId: `bc-019f15ec-e5f5-7517-be9e-9d651e341bd5`
- Ramas: prefijo `cursor/` + sufijo `-1bd5`
- Si el transcript no es accesible, **`PROJECT_MEMORY.json` manda**.

## Norte (no negociable sin override del CEO)
- Firma **autónoma LIVE** (`FIRM_AUTONOMY=true`) con gates: kill-switch, comité/mesa micro, risk desk, mercado abierto, max **$25**/orden.
- Capital **ultra-micro** (~$22): mesa técnica micro + caza continua cuando el comité completo timeout.
- Autopilot ~**10 min** en horario de mercado.
- **2R**: stop 8% / TP 16%; trail arma tras +5%; cooldown post-stop 90 min.
- WhatsApp briefing **3×/día ET**: 09:35 / 12:30 / 16:05 — no spamear.
- Smart EOD: **15:40 ET** cerrar verdes/flat; carry rojos moderados; cortar ≤−8% o tesis sell.

## Host
- Preferir **Railway always-on**; FastAPI Cloud Hobby hiberna y pierde crons.
- SQLite en cloud es efímero → Neon si hay que persistir.

## Pendiente típico al retomar
1. Confirmar Alpaca LIVE + `/api/v1/ops/status`
2. Token dashboard + WhatsApp/Telegram
3. Confirmar prod always-on y Autopilot con mercado abierto
4. Leer `PROJECT_MEMORY.json` → `live_snapshot` y `pending_and_recommended_next`
